<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ConnectHub</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css">
    <style>
        body {
            background-color: #494D4F;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        .container {
            background-color: #272b2d;
            padding: 2rem;
            border-radius: 8px;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
            max-width: 500px;
            width: 100%;
        }
        h1 {
            margin-bottom: 1.5rem;
            color: wheat;
        }
        .form-group label {
            font-weight: bold;
        }
        .btn-primary {
            background-color: #007bff;
            border: none;
            border-radius: 50px;
        }
        .btn-primary:hover {
            background-color: #0056b3;
        }
        .heading {
            display: flex;
            justify-content: space-around;
            align-items: center;
        }
        .heading img {
            height: 100px;
        }
    </style>
</head>
<body>
    <div class="container mt-5">
        <div class="heading">
            <h1>ConnectHub</h1>
            <img src="/images/logo.png" alt="Logo">
        </div>
        <form action="/join-room" method="POST" class="mt-3">
            <div class="form-group">
                <label for="username" style="color: wheat;">Enter Your Name:</label>
                <input type="text" id="username" name="username" class="form-control" required>
            </div>
            <div class="form-group">
                <label for="roomId" style="color: wheat;">Room ID (Leave empty to create a new room):</label>
                <input type="text" id="roomId" name="roomId" class="form-control">
            </div>
            <button type="submit" class="btn btn-primary btn-block">Join</button>
        </form>
    </div>
</body>
</html>
